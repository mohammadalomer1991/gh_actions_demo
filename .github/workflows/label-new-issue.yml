name: learning_workflow

on:
    push:
        branches:
          - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: npm ci
            - run: npm run build --if-present
            - run: echo "execution is being held on"
            - run: npm test